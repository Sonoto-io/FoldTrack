services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    environment:
      VITE_MOCKS_ENABLED : {{ env.MOCKS_ENABLED:-false }}
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev

